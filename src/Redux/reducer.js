import posts from '../Data/posts'
// two arg initial state,action(some action is triggered ex click to change the state of data)

const postReducer= function posts(state=posts,action){
//Remove photo action
return state
}

export default postReducer